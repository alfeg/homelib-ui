name: .NET

on:
  
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    strategy:
        matrix:
          os: [ubuntu-latest, macos-latest, windows-latest]
          
    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run: dotnet restore
      working-directory: FlibustaCli
    - name: Build
      run: dotnet build --no-restore -c release
      working-directory: FlibustaCli

    - name: Small
      run: >
        dotnet publish -c release -p:PublishDir=../dist/small --no-self-contained 
        -p:PublishSingleFile=true -p:DebugType=embedded -p:PublishTrimmed=false 
        -p:EnableCompressionInSingleFile=false --use-current-runtime
      working-directory: FlibustaCli
    - name: Portable
      run: >
        dotnet publish -c release -p:PublishDir=../dist/portable --self-contained 
        -p:PublishSingleFile=true -p:DebugType=embedded -p:PublishTrimmed=true -p:TrimMode=partial
        -p:EnableCompressionInSingleFile=true --use-current-runtime
      working-directory: FlibustaCli

    - uses: actions/upload-artifact@v3
      with:
        name: FlibustaCli-${{ matrix.os }}
        path: ${{ github.workspace }}/dist/small

    - uses: actions/upload-artifact@v3
      with:
        name: FlibustaCli-${{ matrix.os }}-portable
        path: ${{ github.workspace }}/dist/portable
    
  release:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - uses: kzrnm/get-net-sdk-project-versions-action@v1
        id: get-version
        with:
          proj-path: FlibustaCli/FlibustaCli.csproj
          
      - name: "get-last-release"
        id: last_release
        uses: InsonusK/get-latest-release@v1.0.1
        with:
          myToken: ${{ github.token }}
          
      - name: release
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/create-release@v1
        id: create_release
        with:
          draft: false
          prerelease: false
          release_name: ${{steps.get-version.outputs.version}}
          tag_name: ${{steps.get-version.outputs.version}}
        env:
          GITHUB_TOKEN: ${{ github.token }}
         
      - name: download-artifact-ubuntu
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-ubuntu-latest
          path: ${{ github.workspace }}/linux
      - name: download-artifact-ubuntu-portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-ubuntu-latest-portable
          path: ${{ github.workspace }}/linux-portable      
          
      - name: download-artifact-windows
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-windows-latest
          path: ${{ github.workspace }}/windows
      - name: download-artifact-windows-portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-windows-latest-portable
          path: ${{ github.workspace }}/windows-portable
          
      - name: download-artifact-macos
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-macos-latest
          path: ${{ github.workspace }}/macos
      - name: download-artifact-macos-portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/download-artifact@v3
        with:
          name: FlibustaCli-macos-latest-portable
          path: ${{ github.workspace }}/macos-portable
                  
      - name: Install zip
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: montudor/action-zip@v0.1.0
        
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Linux.zip .
        working-directory: ${{ github.workspace }}/linux
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Linux-portable.zip .
        working-directory: ${{ github.workspace }}/linux-portable
          
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Windows.zip .
        working-directory: ${{ github.workspace }}/windows          
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Windows-portable.zip .
        working-directory: ${{ github.workspace }}/windows-portable
        
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Macos.zip .
        working-directory: ${{ github.workspace }}/macos
                 
      - name: Zip output
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        run: zip -qq -x "*.pdb" -r ../FlibustaCli-Macos-portable.zip .
        working-directory: ${{ github.workspace }}/macos-portable
                 
      - name: upload linux artifact
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Linux.zip
          asset_name: FlibustaCli-Linux.zip
          asset_content_type: application/zip
                 
      - name: upload windows artifact
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Windows.zip
          asset_name: FlibustaCli-Windows.zip
          asset_content_type: application/zip
          
      - name: upload macos artifact
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Macos.zip
          asset_name: FlibustaCli-Macos.zip
          asset_content_type: application/zip

      - name: upload linux artifact portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Linux-portable.zip
          asset_name: FlibustaCli-Linux-portable.zip
          asset_content_type: application/zip

      - name: upload windows artifact portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Windows-portable.zip
          asset_name: FlibustaCli-Windows-portable.zip
          asset_content_type: application/zip

      - name: upload macos artifact portable
        if: ${{ steps.last_release.outputs.tag_name != steps.get-version.outputs.version }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: FlibustaCli-Macos-portable.zip
          asset_name: FlibustaCli-Macos-portable.zip
          asset_content_type: application/zip